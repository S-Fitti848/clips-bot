requests>=2.31
PyYAML>=6.0
python-dotenv>=1.0
yt-dlp>=2025.6
faster-whisper>=1.2
numpy>=1.26
# En la Pi (sin pantalla) va la versión headless; no instalar las dos juntas.
# < 5: OpenCV 5.0 sacó CascadeClassifier del módulo principal y toda la detección de cámara
# (layout.py, deportes.py) usa las cascadas de Haar. La Pi instaló 5.0.0 el 2026-09-23 mientras
# Windows tenía 4.x, y el benchmark se cayó con "module 'cv2' has no attribute 'CascadeClassifier'".
opencv-python>=4.8,<5; platform_machine != "aarch64"
opencv-python-headless>=4.8,<5; platform_machine == "aarch64"
# OCR de la capa de datos en pantalla. El binario de tesseract va aparte:
#   Windows: winget install UB-Mannheim.TesseractOCR · Pi/Debian: sudo apt install tesseract-ocr
# Si no está, la capa se saltea con un warning y la corrida sigue.
pytesseract>=0.3.10
pytest>=8.0
# Además: ffmpeg con libass en el PATH (Windows: winget install Gyan.FFmpeg · Pi: apt install ffmpeg)
